# Repository Agent Rules

1. After each completed code change, create a git commit in this repository.
2. Keep commits small and descriptive (one logical change per commit).
3. Before committing, run at least a minimal validation (syntax check or targeted tests).
4. Do not commit temporary comparison artifacts or local scratch files.
